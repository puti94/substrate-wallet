/**
 * User: puti.
 * Time: 2019-12-11 11:09.
 */
import {action, thunk} from 'easy-peasy';

export default {
  list: [],
  addUri: thunk(async (actions, payload) => {}),
  addPair: action((state, payload) => {
  
  }),
};
